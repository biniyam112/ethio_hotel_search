import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFF7643);

const Color buttonColor = Color(0xffFF7C00);
const Color buttonSplashColor = Color(0xFFFF7643);

const Color secondaryColor = Color(0xFF5f3dc4);

const Color textLightColor = Color(0xff68635D);
const Color textDarkColor = Color(0xff333333);

const Shadow defaultShadow = BoxShadow(
  offset: Offset(4, 4),
  color: textLightColor,
  blurRadius: 40,
  spreadRadius: 10,
);

const animationDuration = Duration(milliseconds: 200);
